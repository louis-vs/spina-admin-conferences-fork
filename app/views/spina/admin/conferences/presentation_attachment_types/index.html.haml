= render Spina::UserInterface::HeaderComponent.new do |header|
  - header.actions do
    .ml-3
      = link_to new_admin_conferences_presentation_attachment_type_path, class: 'btn btn-primary w-full' do
        = heroicon 'plus', style: :solid, class: 'w-7 h-7 -m1-2'
        = t '.new'

= render partial: 'presentation_attachment_types', object: @presentation_attachment_types
= link_to_next_page @presentation_attachment_types, 'Next'
